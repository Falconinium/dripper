# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Mapbox
NEXT_PUBLIC_MAPBOX_TOKEN=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Resend (transactional emails for shop claims)
# Leave empty in local dev — the server logs and skips send instead of failing.
RESEND_API_KEY=
EMAIL_FROM="Dripper <hello@dripper.fr>"
